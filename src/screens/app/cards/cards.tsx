import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const Cards = () => {
  return (
    <View>
      <Text style={{ color: '#000'}}>Cards</Text>
    </View>
  );
};

export default Cards;

const styles = StyleSheet.create({

});
